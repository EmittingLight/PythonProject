# Используем базовый образ Python версии 3.8
FROM python:3.8

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем все файлы из текущей директории на хосте в рабочую директорию контейнера
COPY . /app

# Устанавливаем необходимые зависимости (pandas и openpyxl)
RUN pip install pandas openpyxl

# Указываем команду, которая должна выполняться при запуске контейнера
CMD ["python", "main.py"]

